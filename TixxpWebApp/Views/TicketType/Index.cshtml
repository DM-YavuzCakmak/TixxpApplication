@model List<Tixxp.Entities.TicketType.TicketTypeEntity>
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = Localizer["ticketType.INDEX.TITLE"].Value;
}

<div class="main-content app-content">
    <div class="container-fluid">
        <div class="d-md-flex d-block align-items-center justify-content-between my-4 page-header-breadcrumb text-fixed-white">
            <h1 class="page-title fw-medium fs-18 mb-0 text-fixed-white">@Localizer["ticketType.INDEX.TITLE"]</h1>
            @if (User.IsInRole("TIXXP_TICKET_TYPE_ADD"))
            {
                <button class="btn btn-primary btn-sm" onclick="openEditTicketTypeModal(0)">+ @Localizer["ticketType.INDEX.ADD_NEW"]</button>
            }
        </div>

        <div class="row">
            <div class="col-xl-12">
                <div class="card custom-card">
                    <div class="card-header">
                        <div class="card-title">@Localizer["ticketType.INDEX.TICKET_TYPES"]</div>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-bordered text-nowrap table-sm">
                                <thead>
                                    <tr>
                                        <th>@Localizer["ticketType.INDEX.NAME"]</th>
                                        @if (User.IsInRole("TIXXP_TICKET_TYPE_EDIT") || User.IsInRole("TIXXP_TICKET_TYPE_DELETE"))
                                        {
                                            <th>@Localizer["ticketType.INDEX.ACTIONS"]</th>
                                        }
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model != null && Model.Any())
                                    {
                                        foreach (var item in Model)
                                        {
                                            <tr>
                                                <td>@item.Name</td>
                                                @if (User.IsInRole("TIXXP_TICKET_TYPE_EDIT") || User.IsInRole("TIXXP_TICKET_TYPE_DELETE"))
                                                {
                                                    <td>
                                                        <div class="hstack gap-2 fs-15">
                                                            @if (User.IsInRole("TIXXP_TICKET_TYPE_EDIT"))
                                                            {
                                                                <a href="javascript:void(0);" class="btn btn-icon btn-sm btn-light"
                                                                   onclick="openEditTicketTypeModal(@item.Id)"
                                                                   title="@Localizer["ticketType.INDEX.EDIT"]">
                                                                    <i class="ri-edit-line"></i>
                                                                </a>
                                                            }
                                                            @if (User.IsInRole("TIXXP_TICKET_TYPE_DELETE"))
                                                            {
                                                                <a href="javascript:void(0);" class="btn btn-icon btn-sm btn-light"
                                                                   onclick="confirmDeleteTicketType(@item.Id)"
                                                                   title="@Localizer["ticketType.INDEX.DELETE"]">
                                                                    <i class="ri-delete-bin-line"></i>
                                                                </a>
                                                            }
                                                        </div>
                                                    </td>
                                                }
                                            </tr>
                                        }
                                    }
                                    else
                                    {
                                        <tr><td colspan="2" class="text-center">@Localizer["ticketType.INDEX.NO_RECORDS_FOUND"]</td></tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


<!-- Düzenleme Modalı -->
<div class="modal fade" id="editTicketTypeModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-light p-3">
                <h5 class="modal-title" id="editTicketTypeModalTitle"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="editTicketTypeForm">
                    <input type="hidden" id="editTicketTypeId" name="Id" />
                    <div class="mb-3">
                        <label class="form-label">@Localizer["ticketType.INDEX.TICKET_TYPE_NAME"]</label>
                        <input type="text" class="form-control" id="editTicketTypeName" name="Name" required />
                    </div>
                    <button type="submit" class="btn btn-primary w-100">@Localizer["ticketType.INDEX.SAVE"]</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const localizer = {
            areYouSure: '@Localizer["ticketType.INDEX.ARE_YOU_SURE"].Value.Replace("'", "\\'")',
            deleteConfirmation: '@Localizer["ticketType.INDEX.DELETE_CONFIRMATION"].Value.Replace("'", "\\'")',
            yesDelete: '@Localizer["ticketType.INDEX.YES_DELETE"].Value.Replace("'", "\\'")',
            cancel: '@Localizer["ticketType.INDEX.CANCEL"].Value.Replace("'", "\\'")',
            deleted: '@Localizer["ticketType.INDEX.DELETED"].Value.Replace("'", "\\'")',
            success: '@Localizer["ticketType.INDEX.SUCCESS"].Value.Replace("'", "\\'")',
            error: '@Localizer["ticketType.INDEX.ERROR"].Value.Replace("'", "\\'")',
            serverError: '@Localizer["ticketType.INDEX.SERVER_ERROR"].Value.Replace("'", "\\'")',
            dataError: '@Localizer["ticketType.INDEX.DATA_ERROR"].Value.Replace("'", "\\'")',
            addTicketType: '@Localizer["ticketType.INDEX.ADD_TICKET_TYPE"].Value.Replace("'", "\\'")',
            editTicketType: '@Localizer["ticketType.INDEX.EDIT_TICKET_TYPE"].Value.Replace("'", "\\'")'
        };

        function confirmDeleteTicketType(id) {
            Swal.fire({
                title: localizer.areYouSure,
                text: localizer.deleteConfirmation,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                confirmButtonText: localizer.yesDelete,
                cancelButtonText: localizer.cancel
            }).then((result) => {
                if (result.isConfirmed) {
                    $.post('/TicketType/Delete', { id: id }, function (response) {
                        if (response.success) {
                            Swal.fire(localizer.deleted, response.message, "success").then(() => location.reload());
                        } else {
                            Swal.fire(localizer.error, response.message, "error");
                        }
                    }).fail(() => Swal.fire(localizer.error, localizer.serverError, "error"));
                }
            });
        }

        function openEditTicketTypeModal(id) {
            const modalTitle = $('#editTicketTypeModalTitle');
            if (id === 0) {
                $('#editTicketTypeForm')[0].reset();
                $('#editTicketTypeId').val(0);
                modalTitle.text(localizer.addTicketType);
                $('#editTicketTypeModal').modal('show');
                return;
            }

            $.get('/TicketType/GetById', { id: id }, function (data) {
                $('#editTicketTypeId').val(data.id);
                $('#editTicketTypeName').val(data.name);
                modalTitle.text(localizer.editTicketType);
                $('#editTicketTypeModal').modal('show');
            }).fail(() => Swal.fire(localizer.error, localizer.dataError, "error"));
        }

        $('#editTicketTypeForm').submit(function (e) {
            e.preventDefault();
            const id = Number($('#editTicketTypeId').val());
            const data = {
                Id: id,
                Name: $('#editTicketTypeName').val()
            };

            const url = id === 0 ? '/TicketType/Create' : '/TicketType/Update';

            $.ajax({
                url: url,
                method: 'POST',
                contentType: 'application/json',
                data: JSON.stringify(data),
                success: function (response) {
                    if (response.success) {
                        Swal.fire(localizer.success, response.message, "success").then(() => location.reload());
                    } else {
                        Swal.fire(localizer.error, response.message, "error");
                    }
                },
                error: () => Swal.fire(localizer.error, localizer.serverError, "error")
            });
        });
    </script>
}
